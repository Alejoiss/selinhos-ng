import { CellphonePrivacyMaskPipe } from './cellphone-privacy-mask.pipe';

describe('CellphonePrivacyMaskPipe', () => {
    it('create an instance', () => {
        const pipe = new CellphonePrivacyMaskPipe();
        expect(pipe).toBeTruthy();
    });
});
