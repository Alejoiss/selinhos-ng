import { ManualStampRegister } from './manual-stamp-register';

describe('ManualStampRegister', () => {
  it('should create an instance', () => {
    expect(new ManualStampRegister()).toBeTruthy();
  });
});
