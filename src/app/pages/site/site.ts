import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
    selector: 'app-site',
    imports: [
        RouterModule
    ],
    templateUrl: './site.html',
    styleUrl: './site.scss'
})
export class Site {
}
