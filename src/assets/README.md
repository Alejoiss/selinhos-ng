# Project assets

Place images and public assets here. Files inside this directory will be copied to `/assets/` on build and served publicly by the application.

Examples:
- `logo.png`
- `icons/`

This folder is added to `angular.json` so assets are available under `/assets/` at runtime.
